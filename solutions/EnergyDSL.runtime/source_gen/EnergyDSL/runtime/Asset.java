package EnergyDSL.runtime;

/*Generated by MPS */

import java.util.Map;
import java.util.HashMap;
import org.json.JSONObject;

public class Asset {
  protected String asset = "";

  protected Asset() {
    this.asset = "0";
  }
  public Asset(String test) {
    this.asset = test;
  }
  public String getAsset() {
    return asset;
  }

  @Override
  public boolean equals(Object o) {
    if (!(o instanceof Asset)) {
      return false;
    }
    Asset ast = ((Asset) o);

    if (!(this.asset.equals(ast.getAsset()))) {
      return false;
    }

    return true;
  }

  @Override
  public String toString() {
    String result = "";
    result = result.concat("asset" + ":" + asset + "; ");
    return result;
  }
  public void setAsset(String test) {
    this.asset = test;
  }

  public String seralize() {
    Map<String, Object> tMap = new HashMap<String, Object>();
    tMap.put("asset", this.asset);
    return new JSONObject(tMap).toString();
  }

  public static Asset deserialize(String posNumJSON) {
    JSONObject json = new JSONObject(posNumJSON);
    String asset = json.getString("asset");
    return new Asset(asset);
  }
}
