package EnergyDSLModels.StateMachinesWithCore;

/*Generated by MPS */

import EnergyDSL.runtime.StateMachine;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import jetbrains.mps.baseLanguage.closures.runtime._FunctionTypes;
import java.math.BigInteger;
import org.pcollections.PVector;
import org.pcollections.TreePVector;
import java.util.logging.Logger;
import EnergyDSL.runtime.State;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import EnergyDSL.runtime.Transition;
import java.util.Map;
import org.iets3.core.expr.simpleTypes.runtime.AH;
import java.util.HashMap;

public class TradingStateMachine extends StateMachine {


  public enum StateEnum {
    INIT("Init"),
    REGISTER("Register"),
    BIDDING("Bidding"),
    TRADING("Trading"),
    FINISHED("Finished");

    /**
     * * Human-readable representation of the state.
     */
    private final String value;

    /**
     *  * Creates a state enum constant.
     *  @param value display value of the state
     */
    StateEnum(String value) {
      this.value = value;
    }

    /**
     *  * Returns the display value of the state.
     *  @return state display value
     */
    public String getValue() {
      return value;
    }
    /**
     *  * Converts a string value to the corresponding enum constant.
     *  @param value state value
     *  @return matching enum constant
     *  @throws IllegalArgumentException if no matching state exists
     */
    public static StateEnum fromValue(String value) {
      for (StateEnum state : StateEnum.values()) {
        if (state.value.equalsIgnoreCase(value)) {
          return state;
        }
      }
      throw new IllegalArgumentException("Unexpected state value: " + value);
    }
  }
  public static Number updatePositiveInteger(Number num, final Number v) {
    final Wrappers._T<Number> _num = new Wrappers._T<Number>(num);
    Number res = new _FunctionTypes._return_P0_E0<Number>() {
      public Number invoke() {
        return _num.value = v;
      }
    }.invoke();

    return res;
  }
  public static Boolean changeReset(final Boolean v) {
    Boolean res = new _FunctionTypes._return_P0_E0<Boolean>() {
      public Boolean invoke() {
        return TradingStateMachine.restart = v;
      }
    }.invoke();

    return res;
  }
  public static Number updatePositiveNumber(Number num, final Number v) {
    final Wrappers._T<Number> _num = new Wrappers._T<Number>(num);
    Number res = new _FunctionTypes._return_P0_E0<Number>() {
      public Number invoke() {
        return _num.value = v;
      }
    }.invoke();

    return res;
  }

  public static Number energyValue = ((Number) new BigInteger("10"));
  public static Number pmax = ((Number) new BigInteger("10000"));
  public static Boolean trade = false;
  public static Number balance = ((Number) new BigInteger("1000"));
  public static StateEnum state = StateEnum.INIT;
  public static Number smcAddr = ((Number) new BigInteger("12121212"));
  public static PVector<EnergyOwnership> energyRecording = new _FunctionTypes._return_P0_E0<TreePVector<EnergyOwnership>>() {
    public TreePVector<EnergyOwnership> invoke() {
      TreePVector<EnergyOwnership> tpv = TreePVector.empty();
      tpv = tpv.plus(new EnergyOwnership(TradingStateMachine.smcAddr, ((Number) new BigInteger("0")), Core.State.initial.getValue(), ((Number) new BigInteger("1738441000000")), ((Number) new BigInteger("0"))));
      return tpv;
    }
  }.invoke();
  public static Boolean restart = false;
  public static MyConsumer consumer = new MyConsumer(((Number) new BigInteger("1000")), ((Number) new BigInteger("1000")));
  public static Number id = ((Number) new BigInteger("1"));
  public static Number timestamp = ((Number) new BigInteger("1738441000000"));
  public static MyProsumer prosumer = new MyProsumer(((Number) new BigInteger("1000")), ((Number) new BigInteger("1000")));

  private static final Logger logger = Logger.getLogger("StateMachine");

  public TradingStateMachine() {
    super(new State());
    MapSequence.fromMap(states).put("initialize", startState);

    MapSequence.fromMap(states).put("registered", new State("registered"));
    MapSequence.fromMap(states).put("bid", new State("bid"));
    MapSequence.fromMap(states).put("trading", new State("trading"));
    MapSequence.fromMap(states).put("finished", new State("finished"));

    MapSequence.fromMap(states).get("initialize").addTransition("initial", new Transitioninitialinitialize(MapSequence.fromMap(states).get("registered")));
    MapSequence.fromMap(states).get("registered").addTransition("register", new Transitionregisterregistered(MapSequence.fromMap(states).get("bid")));
    MapSequence.fromMap(states).get("bid").addTransition("bidding", new Transitionbiddingbid(MapSequence.fromMap(states).get("trading")));
    MapSequence.fromMap(states).get("trading").addTransition("trading", new Transitiontradingtrading(MapSequence.fromMap(states).get("finished")));
    MapSequence.fromMap(states).get("finished").addTransition("finished", new Transitionfinishedfinished(MapSequence.fromMap(states).get("initialize")));

  }
  public static class Transitioninitialinitialize extends Transition {
    public Transitioninitialinitialize(State state) {
      super(state);
    }
    @Override
    public State fireEvent(Map<String, Object> parameters, Object vr) {
      boolean conditionsFullfilled = true;



      if (!(conditionsFullfilled)) {
        return null;
      }
      new _FunctionTypes._return_P0_E0<StateEnum>() {
        public StateEnum invoke() {
          TradingStateMachine.energyRecording = TradingStateMachine.energyRecording.plus(TradingStateMachine.id.intValue(), new EnergyOwnership(((Number) new BigInteger("987654321")), TradingStateMachine.balance, Core.State.register.getValue(), TradingStateMachine.timestamp, ((Number) new BigInteger("0"))));
          TradingStateMachine.state = StateEnum.REGISTER;
          TradingStateMachine.balance = ((Number) new BigInteger("200"));
          TradingStateMachine.id = AH.add(TradingStateMachine.id, ((Number) new BigInteger("1")));
          return TradingStateMachine.state;

        }
      }.invoke();

      return targetState;
    }
  }
  public static class Transitionregisterregistered extends Transition {
    public Transitionregisterregistered(State state) {
      super(state);
    }
    @Override
    public State fireEvent(Map<String, Object> parameters, Object vr) {
      boolean conditionsFullfilled = true;


      if (!(AH.isLess(TradingStateMachine.energyValue, TradingStateMachine.pmax))) {
        conditionsFullfilled = false;
      }

      if (!(conditionsFullfilled)) {
        return null;
      }
      new _FunctionTypes._return_P0_E0<StateEnum>() {
        public StateEnum invoke() {
          TradingStateMachine.energyRecording = TradingStateMachine.energyRecording.plus(TradingStateMachine.id.intValue(), new EnergyOwnership(((Number) new BigInteger("987654321")), TradingStateMachine.balance, Core.State.bidding.getValue(), TradingStateMachine.timestamp, TradingStateMachine.energyValue));
          TradingStateMachine.state = StateEnum.BIDDING;
          TradingStateMachine.id = AH.add(TradingStateMachine.id, ((Number) new BigInteger("1")));
          TradingStateMachine.trade = true;
          return TradingStateMachine.state;
        }
      }.invoke();

      return targetState;
    }
  }
  public static class Transitionbiddingbid extends Transition {
    public Transitionbiddingbid(State state) {
      super(state);
    }
    @Override
    public State fireEvent(Map<String, Object> parameters, Object vr) {
      boolean conditionsFullfilled = true;


      if (!(TradingStateMachine.trade == true)) {
        conditionsFullfilled = false;
      }

      if (!(conditionsFullfilled)) {
        return null;
      }
      new _FunctionTypes._return_P0_E0<StateEnum>() {
        public StateEnum invoke() {
          TradingStateMachine.energyRecording = TradingStateMachine.energyRecording.plus(TradingStateMachine.id.intValue(), new EnergyOwnership(((Number) new BigInteger("987654321")), TradingStateMachine.balance, Core.State.purchased.getValue(), TradingStateMachine.timestamp, TradingStateMachine.energyValue));
          TradingStateMachine.state = StateEnum.TRADING;
          TradingStateMachine.id = AH.add(TradingStateMachine.id, ((Number) new BigInteger("1")));
          return TradingStateMachine.state;
        }
      }.invoke();

      return targetState;
    }
  }
  public static class Transitiontradingtrading extends Transition {
    public Transitiontradingtrading(State state) {
      super(state);
    }
    @Override
    public State fireEvent(Map<String, Object> parameters, Object vr) {
      boolean conditionsFullfilled = true;



      if (!(conditionsFullfilled)) {
        return null;
      }
      new _FunctionTypes._return_P0_E0<StateEnum>() {
        public StateEnum invoke() {
          TradingStateMachine.prosumer = ((MyProsumer) new MyProsumer(AH.add(TradingStateMachine.prosumer.getAmount(), TradingStateMachine.balance), AH.sub(TradingStateMachine.prosumer.getEnergy(), TradingStateMachine.energyValue)));
          TradingStateMachine.consumer = ((MyConsumer) new MyConsumer(AH.sub(TradingStateMachine.consumer.getAmount(), TradingStateMachine.balance), AH.add(TradingStateMachine.consumer.getEnergy(), TradingStateMachine.energyValue)));
          TradingStateMachine.state = StateEnum.FINISHED;
          return TradingStateMachine.state;
        }
      }.invoke();

      return targetState;
    }
  }
  public static class Transitionfinishedfinished extends Transition {
    public Transitionfinishedfinished(State state) {
      super(state);
    }
    @Override
    public State fireEvent(Map<String, Object> parameters, Object vr) {
      boolean conditionsFullfilled = true;


      if (!(TradingStateMachine.restart == true)) {
        conditionsFullfilled = false;
      }

      if (!(conditionsFullfilled)) {
        return null;
      }
      new _FunctionTypes._return_P0_E0<StateEnum>() {
        public StateEnum invoke() {
          TradingStateMachine.trade = false;
          TradingStateMachine.balance = ((Number) new BigInteger("0"));
          TradingStateMachine.state = StateEnum.INIT;
          TradingStateMachine.energyValue = ((Number) new BigInteger("0"));

          return TradingStateMachine.state;
        }
      }.invoke();

      return targetState;
    }
  }
  public static void main(String[] args) {
    TradingStateMachine stateMachine = new TradingStateMachine();
    logger.info("Initial state: " + stateMachine.getStartState().toString());
    Map<String, Object> params = new HashMap<>();
    State current = stateMachine.getStartState();
    current = current.fireTransition("initial", params);
    if (current == null) {
      logger.warning("Transition 'initial' failed - condition not met");
      return;
    }

    logger.info("Before trading Consumer: " + consumer.toString());
    logger.info("Before trading Prosumer: " + prosumer.toString());
    logger.info("After 'initial' event -> state: " + current);
    current = current.fireTransition("register", params);
    if (current == null) {
      logger.warning("Transition 'register' failed - energyValue >= pmax");
      return;
    }

    logger.info("After 'register' event -> state: " + current);
    current = current.fireTransition("bidding", params);
    if (current == null) {
      logger.warning("Transition bidding");
      return;
    }

    logger.info("After 'bidding' event -> state: " + current);
    current = current.fireTransition("trading", params);
    if (current == null) {
      logger.warning("Transition trading failed");
      return;
    }

    logger.info("After 'trading' event -> state: " + current);

    if (consumer != null) {
      logger.info("After trading Consumer: " + consumer);
    }
    if (prosumer != null) {
      logger.info("After trading Prosumer: " + prosumer);
    }
  }

}
