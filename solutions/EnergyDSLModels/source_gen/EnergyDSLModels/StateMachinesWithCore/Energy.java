package EnergyDSLModels.StateMachinesWithCore;

/*Generated by MPS */

import EnergyDSL.runtime.Asset;
import java.lang.reflect.Field;
import com.owlike.genson.Genson;
import com.owlike.genson.GenericType;
import java.util.Map;
import java.util.HashMap;
import org.json.JSONObject;
import java.util.Objects;

/**
 * Concept that is a type of Asset that implements IRecodedDeclaration
 */
public class Energy extends Asset {
  public Energy(Object... args) {
    try {
      Field[] fields = this.getClass().getDeclaredFields();
      for (int i = 1; i - 1 < args.length; i++) {
        fields[i - 1].set(this, args[i - 1]);
      }
    } catch (IllegalAccessException e) {
      e.printStackTrace();
    }
  }
  public Number amount;
  public String state;
  public Number timestamp;
  public Number getAmount() {
    return amount;
  }
  public String getState() {
    return state;
  }
  public Number getTimestamp() {
    return timestamp;
  }
  public void setAmount(Number amount) {
    this.amount = amount;
  }
  public void setState(String state) {
    this.state = state;
  }
  public void setTimestamp(Number timestamp) {
    this.timestamp = timestamp;
  }

  public static Energy deserialize(String jsonObject) {
    Energy asset = new Genson().deserialize(jsonObject, new GenericType<Energy>() {});
    return asset;
  }

  public String serialize() {
    Map<String, Object> map = new HashMap<>();
    map.put("amount", amount);
    map.put("state", state);
    map.put("timestamp", timestamp);

    return new JSONObject(map).toString();
  }
  public String toString() {
    Map<String, Object> map = new HashMap<>();
    map.put("amount", amount);
    map.put("state", state);
    map.put("timestamp", timestamp);

    return new JSONObject(map).toString();
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof Energy)) {
      return false;
    }
    Energy rd = (Energy) o;
    boolean result = true;
    result = result && Objects.equals(amount, rd.amount);
    result = result && Objects.equals(state, rd.state);
    result = result && Objects.equals(timestamp, rd.timestamp);
    return result;
  }
  @Override
  public int hashCode() {
    return Objects.hash(amount, state, timestamp);
  }
}
