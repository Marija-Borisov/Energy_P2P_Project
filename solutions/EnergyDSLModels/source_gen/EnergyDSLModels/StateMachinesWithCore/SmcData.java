package EnergyDSLModels.StateMachinesWithCore;

/*Generated by MPS */

import EnergyDSL.runtime.Asset;
import java.lang.reflect.Field;
import com.owlike.genson.Genson;
import com.owlike.genson.GenericType;
import java.util.Map;
import java.util.HashMap;
import org.json.JSONObject;
import java.util.Objects;

/**
 * Concept that is a type of Asset that implements IRecodedDeclaration
 */
public class SmcData extends Asset {
  public SmcData(Object... args) {
    try {
      Field[] fields = this.getClass().getDeclaredFields();
      for (int i = 1; i - 1 < args.length; i++) {
        fields[i - 1].set(this, args[i - 1]);
      }
    } catch (IllegalAccessException e) {
      e.printStackTrace();
    }
  }
  public String regPros;
  public String regCons;
  public String getRegPros() {
    return regPros;
  }
  public String getRegCons() {
    return regCons;
  }
  public void setRegPros(String regPros) {
    this.regPros = regPros;
  }
  public void setRegCons(String regCons) {
    this.regCons = regCons;
  }

  public static SmcData deserialize(String jsonObject) {
    SmcData asset = new Genson().deserialize(jsonObject, new GenericType<SmcData>() {});
    return asset;
  }

  public String serialize() {
    Map<String, Object> map = new HashMap<>();
    map.put("regPros", regPros);
    map.put("regCons", regCons);

    return new JSONObject(map).toString();
  }
  public String toString() {
    Map<String, Object> map = new HashMap<>();
    map.put("regPros", regPros);
    map.put("regCons", regCons);

    return new JSONObject(map).toString();
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof SmcData)) {
      return false;
    }
    SmcData rd = (SmcData) o;
    boolean result = true;
    result = result && Objects.equals(regPros, rd.regPros);
    result = result && Objects.equals(regCons, rd.regCons);
    return result;
  }
  @Override
  public int hashCode() {
    return Objects.hash(regPros, regCons);
  }
}
